@page "/Register"

@using HermesTravel.Data
@using DataAcessHermesTravel
@*@using HermesTravel.Pages.Loads*@
@using Microsoft.Extensions.Configuration
@inject IConfiguration _config
@inject NavigationManager helper
@inject User user

@code 
{
    int result = 0;
    //LoadRegister load = new LoadRegister();

    private async Task InsertRegisto()
    {
        //user = new User(load.email, load.password, load.name);
        //strin        
    }
}